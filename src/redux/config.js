const REDUCER_PATHS = {
  WS: 'ws',
  ROUTER: 'router',
  META: 'meta',
  UI: 'ui',
  NOTIFICATIONS: 'notifications',
}

const MAX_STORED_TRADES = 25

export {
  REDUCER_PATHS, // eslint-disable-line
  MAX_STORED_TRADES,
}

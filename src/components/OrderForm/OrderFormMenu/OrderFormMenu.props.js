import PropTypes from 'prop-types'

export const propTypes = {
  atomicOrderTypes: PropTypes.array.isRequired,
  algoOrderTypes: PropTypes.array.isRequired,
  onSelect: PropTypes.func.isRequired,
}

export const defaultProps = {

}

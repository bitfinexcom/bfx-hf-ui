/**
 * A `redux` action
 *
 * @typedef {object} ReduxAction
 * @property {object} meta - metadata (differs per action)
 * @property {string} type - action type
 * @property {object} payload - action payload/data
 */
